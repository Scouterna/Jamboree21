apiVersion: apps/v1
kind: Deployment
metadata:
  annotations: {}
  name: facelist
spec:
  replicas: 2
  selector:
    matchLabels:
      app: facelist
  template:
    metadata:
      labels:
        app: facelist
    spec:
      containers:
        - image: %IMAGE%
          imagePullPolicy: Always
          name: facelist
          volumeMounts:
            - mountPath: "/"
              name: facelist-config
              readOnly: true
      volumes:
        - name: facelist-config
          secret:
            secretName: facelist-config
